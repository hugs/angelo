playlist("Playlist Name")

beforeEach(function(){
  // Steps to run before each workflow go here
  // A great place to launch a browser
})

afterEach(function(){
  // Steps to run after each workflow go here
  // A great place to close a browser
})

workflow("Workflow #1", function(){
  assert(1)
})

workflow("Workflow #2", function(){
  assert(1)
})